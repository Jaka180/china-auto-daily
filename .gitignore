# 凭证 / 密钥 —— 绝不提交
server/config.json
server/.env
*.log

# 服务器端每日存档（只留在服务器，不进仓库）
archive/

# Python
__pycache__/
*.pyc
.venv/
venv/

# 网站工作副本（单独 push 到 Jaka180/tochinacar，见 mac/publish_site.sh）
topchinacar-site/
